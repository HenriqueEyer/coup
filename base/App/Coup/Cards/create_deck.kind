App.Coup.Cards.create_deck(deck: List<Pair<Nat, App.Coup.Character>>): App.Coup.Deck
  list = []
  for pair in deck with list:
    let {count, character} = pair
    for i from 0 to count with list:
      App.Coup.Card.new(character, false, i) & list
    list
  list